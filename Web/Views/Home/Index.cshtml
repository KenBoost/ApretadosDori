@model IEnumerable<Infraestructure.Model.Apretados>

@{
    ViewBag.Title = "Home";
}

<head>
    <link rel="stylesheet" type="text/css" href="~/Content/CSSApretados/EstiloApretados.css">
</head>

<!-- Example Code -->


<div id="carouselExampleCaptions" class="carousel slide">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
    <div class="carousel-inner">
     
        @foreach (var helado in Model)
        {
            <div class="carousel-item active">
                <img src="data:image/jpeg;charset=utf-8;base64,@Convert.ToBase64String(helado.imagen)"  role="img" style="max-height:500px">
                <div class="carousel-caption d-none d-md-block">
                    <h5>@Html.DisplayFor(modelItem => helado.Nombre)</h5>
                </div>

            </div>
        }
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>

</div>

